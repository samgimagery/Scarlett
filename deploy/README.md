# Deploy

Placeholder for future auto-install packaging.

Future target:

```bash
./deploy/install.sh --customer customers/ams/customer.yaml
```

For now, this folder documents the intended deployment surface without changing the current working AMS runtime.
