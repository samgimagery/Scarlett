# Customer Instances

Each business gets its own isolated instance:

```text
customers/<customer_id>/
  customer.yaml
  vault/
  sources/
  rules/
  service-profile.md
  deterministic-facts.yaml
  channels/
  logs/
  tests/
  deployment/
```

Sam keeps the cockpit. The customer gets the working receptionist.
